import React from "react";

export default function Header(props){

    return (
        <header>
            <div>
                <button onClick={this.logg}
            </div>
        </header>
    )
}
